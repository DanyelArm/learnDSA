---
phase: 03-gamification
plan: 05
type: execute
wave: 4
depends_on: [03-02, 03-03, 03-04]
files_modified: []
autonomous: false
requirements: [GAME-01, GAME-02, GAME-03, GAME-04, GAME-05, GAME-06, GAME-07]

must_haves:
  truths:
    - "Completing a quiz awards 50 XP and updates the Navbar XP bar"
    - "Completing practice awards 100 XP"
    - "Completing challenge awards 200 XP"
    - "First-try bonus of 50 XP works"
    - "Level-up modal fires on XP threshold crossing"
    - "Badge modal fires on achievement unlock"
    - "Level-up plays before badge when both trigger"
    - "Hint gate shows mini-challenge before revealing hint"
    - "3 hint tiers display in order"
  artifacts: []
  key_links: []

scope_notes:
  - "GAME-07 'map node reveal animation': deferred to Phase 5 polish. Phase 2 already provides visual node state updates (locked/available/complete styling via nodeState). Phase 3 gamification scope is limited to level-up modal and badge modal animations per CONTEXT.md decisions D-21/D-22."
---

<objective>
End-to-end verification of the entire gamification system: XP awards, level-ups, achievements, hints, and animations all working together through the real UI.

Purpose: Catch integration issues that unit tests miss — API response shape mismatches, store wiring gaps, animation sequencing bugs.
Output: Human-verified gamification flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-gamification/03-CONTEXT.md
@.planning/phases/03-gamification/03-01-SUMMARY.md
@.planning/phases/03-gamification/03-02-SUMMARY.md
@.planning/phases/03-gamification/03-03-SUMMARY.md
@.planning/phases/03-gamification/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any integration issues</name>
  <files></files>
  <read_first>
    dsa-quest/server/src/controllers/topicsController.ts,
    dsa-quest/client/src/components/gamification/GamificationOrchestrator.tsx,
    dsa-quest/client/src/components/gamification/HintGate.tsx,
    dsa-quest/client/src/components/ui/Navbar.tsx
  </read_first>
  <action>
    Run the full test suite for both server and client:
    ```bash
    cd dsa-quest && npm run test --workspace=server -- --run && npm run test --workspace=client -- --run
    ```

    If any tests fail, diagnose and fix the root cause. Common integration issues to check:
    - TypeScript compilation errors from type mismatches between shared types and actual API responses
    - Import path issues for new gamification components
    - Missing mock setup in test files
    - Prisma client not regenerated after schema changes (run `npx prisma generate`)

    Also verify TypeScript compiles for both workspaces:
    ```bash
    npx tsc --noEmit --project server/tsconfig.json
    npx tsc --noEmit --project client/tsconfig.json
    ```

    Seed the database to ensure Achievement rows exist:
    - Check if the seed script populates the Achievement table. If not, add achievement seeding logic that inserts all 12 ACHIEVEMENTS from `shared/constants.ts`.
    - Run `npx prisma db seed` to verify.

    Start the dev server to confirm no runtime crashes:
    ```bash
    npm run dev
    ```
    Verify both client (5173) and server (3001) start without errors.
  </action>
  <verify>
    <automated>cd /home/danyelarm/projects/learnDSA/dsa-quest && npm run test --workspace=server -- --run 2>&1 | tail -5 && npm run test --workspace=client -- --run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test --workspace=server -- --run` exits 0
    - `npm run test --workspace=client -- --run` exits 0
    - `npx tsc --noEmit --project server/tsconfig.json` exits 0
    - `npx tsc --noEmit --project client/tsconfig.json` exits 0
    - `npm run dev` starts both client and server without crash
  </acceptance_criteria>
  <done>
    All server and client tests pass. TypeScript compiles without errors. Dev server starts cleanly. Database seeded with achievements.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of gamification flow</name>
  <files></files>
  <action>
    Present the verification checklist to the user. The app should already be running from Task 1. Guide the user through each test scenario below.
  </action>
  <what-built>
    Complete gamification system: XP awards on stage completion, level-up detection with scroll modal animation, achievement unlocks with badge flip modal, hint system gated behind mini-challenges, and persistent XP bar in Navbar.
  </what-built>
  <how-to-verify>
    Start the app: `cd dsa-quest && npm run dev`
    Open browser to http://localhost:5173

    **Test 1 — XP Award + Navbar update (GAME-01):**
    1. Register a fresh account or use an existing one
    2. Navigate to Big-O topic (Topic 1)
    3. Read theory, then take the quiz — answer correctly (>= 80%)
    4. After quiz pass, observe: Navbar XP bar should update from 0 to 50+ XP (50 base + 50 first-try if first attempt)
    5. Confirm the XP bar shows the new value without page reload

    **Test 2 — Level-up modal (GAME-02):**
    6. Continue through practice exercise — submit correct solution
    7. Continue through challenge exercise — submit correct solution
    8. At some point the cumulative XP should cross the level 2 threshold (xpForLevel(2) = 282)
    9. When it does: a parchment scroll modal should unfurl from top of screen saying "Level Up! You are now Level 2"
    10. Click to dismiss

    **Test 3 — Achievement badge modal (GAME-04, GAME-07):**
    11. Completing Topic 1 should trigger ACH-01 "First Steps" and ACH-02 "Arena Champion"
    12. After level-up modal dismisses, badge modal(s) should appear with coin-flip animation
    13. Badge modal should show achievement name and description
    14. Confirm badge modal is visually distinct from level-up modal (smaller, different animation)

    **Test 4 — Sequential modals (GAME-07):**
    15. If both level-up AND badge fire in the same action, confirm level-up appears FIRST
    16. After dismissing level-up, badge modal appears

    **Test 5 — Hint system (GAME-05, GAME-06):**
    17. Start a new topic's practice or return to Big-O
    18. In the editor view, find the Hints section
    19. Tier 1 should be locked — click "Prove Your Worth"
    20. A mini-challenge should appear (one of 3 types: multiple-choice, code-trace, or fill-blank)
    21. Answer correctly — hint Tier 1 should unlock and show the approach hint
    22. Click "Prove Your Worth" for Tier 2 — complete another mini-challenge
    23. Confirm Tier 2 shows pseudocode hint
    24. Repeat for Tier 3 — confirm it shows the key code snippet

    **Test 6 — Speed bonus (GAME-03):**
    25. If the practice/challenge has a timeBonusThreshold, complete it quickly
    26. Check that extra XP was awarded (visible in Navbar bar jump)
  </how-to-verify>
  <verify>Human confirms all 6 test scenarios pass</verify>
  <acceptance_criteria>
    - XP bar updates reactively in Navbar after quiz/exercise completion
    - Level-up modal unfurls as parchment scroll with "Level Up!" text
    - Badge modal flips in with achievement name and description
    - Level-up modal appears before badge modal when both trigger
    - Hint gate shows "Prove Your Worth" for locked tiers
    - Mini-challenge renders correctly for all 3 types
    - All 3 hint tiers reveal in correct order after mini-challenge pass
  </acceptance_criteria>
  <done>
    User has verified all gamification features work end-to-end. Phase 3 is complete.
  </done>
  <resume-signal>Type "approved" to complete Phase 3, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- All automated tests pass
- Human verifies full gamification flow end-to-end
- XP awards, level-ups, achievements, hints, and animations all function correctly
</verification>

<success_criteria>
- XP bar in Navbar updates reactively on stage completion
- Level-up scroll modal plays on XP threshold crossing
- Achievement badge modal plays with coin-flip animation
- Sequential modal ordering works (level-up before badge)
- Hint gate requires mini-challenge before revealing each tier
- All 3 hint tiers display correctly
- No regressions in existing lesson flow
</success_criteria>

<output>
After completion, create `.planning/phases/03-gamification/03-05-SUMMARY.md`
</output>
